<div class="container mt-5">
  <h4>
    <span class="bg-light">商品詳細</span>
  </h4>

  <div class="row">
    <!--div classの間が半角スペースでないとclassが作用しない。ctrl shift i でクラスのあと""の中身が青いと作用している。紫は通常のタグ認識。-->
    <div class="col-md-3 py-3">
      <% if @item.image.attached? %>
        <%= image_tag @item.image, size: "350x300", class: "img-fluid img-thumbnail" %>
      <% else %>
        <%= image_tag 'no_image', size: "300x300", class: "img-fluid" %>
      <% end %>
    </div>

    <div class="col-md-4">
      <p class="ml-5 my-3">商品名</p>
      <p class="ml-5 my-3">商品説明</p>
      <p class="ml-5 my-3">ジャンル</p>
      <p class="ml-5 my-3">税込価格(税抜価格)</p>
      <p class="ml-5 my-3">販売ステータス</p>
    </div>

    <div class="col-md-4">
      <p class="my-3"><%= @item.name %></p>
      <p class="my-3"><%= @item.introduction %></p>
      <p class="my-3"><%= @item.genre.name %></p>
      <p class="my-3">
          <%= @item.with_tax_price.to_s(:dalimited) %>
          (<%= @item.price %>)円
      </p>
      <p class="my-2">
          <% if @item.is_active == true %>
            <span class="text-success font-weight-bold">販売中</span>
          <% else %>
            <span class="text-secondary font-weight-bold">販売停止中</span>
          <% end %>
      </p>
    </div>
  </div>
  <div class="col-md-8 text-center">
      <p><%= link_to "編集する", edit_admin_item_path(@item.id), class: "mx-5 my-5 col-md-4 btn btn-success" %></p>
    </div>
</div>